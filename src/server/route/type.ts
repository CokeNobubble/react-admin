export type MenuData = {
  component: string,
  icon: string | null,
  id?: number,
  path: string,
  permission: string[],
  pid: number,
  title: string,
}


export type Icons = {
  name: string;
};
